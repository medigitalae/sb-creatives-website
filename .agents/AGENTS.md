# Behavioral Guidelines

- **Git Usage**: Do not automatically push changes to git (e.g. `git push`) without explicit permission from the user. You may stage and commit changes locally if helpful, but you must ask for approval before pushing to the remote repository.
